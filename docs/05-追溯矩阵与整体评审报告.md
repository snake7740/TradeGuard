# 05 · 追溯矩阵与整体评审报告

> 本文档是 TradeGuard 4A 文档集（[00](./00-总则.md) / [01 BA](./01-业务架构BA.md) / [02 AA](./02-应用架构AA.md) / [03 DA](./03-数据架构DA.md) / [04 TA](./04-技术架构TA.md)）的横向校验与评审结论。

---

## 1. BA → AA → DA → TA 全链路追溯矩阵

| 业务阶段（BA） | 承接 Agent（AA） | 使用 Skill（AA） | 工具（AA-MCP） | 读写数据（DA） | 承载组件（TA） |
|---|---|---|---|---|---|
| BA-BP-01 A 告警接入 | AA-AG-01 | — | — | DA-T-03 写 | TA-C-01 + TA-C-06 |
| BA-BP-02 信号聚合降噪 | AA-AG-02 | AA-SK-01 | AA-MCP-01/02 | DA-T-02 读、DA-T-04 写 | TA-C-03 + TA-C-04 |
| BA-BP-01 C 风险分级 | AA-AG-01 | — | — | DA-T-03 读写 | TA-C-01 |
| BA-BP-03 根因与影响面 | AA-AG-03 | AA-SK-02 | AA-MCP-01 | DA-T-05 写、DA-KB-01 读 | TA-C-08 + TA-C-03 |
| BA-BP-01 G-I 审批 | 人类审批官 | — | — | DA-T-07 写 | TA-C-01 Matrix 房间 |
| BA-BP-01 H 处置执行 | AA-AG-04 | AA-SK-03 | AA-MCP-01 | DA-T-06 写 | TA-C-03 + TA-C-04 |
| BA-BP-01 K 核验审计 | AA-AG-05 | AA-SK-04 | AA-MCP-01 | DA-T-08 写 | TA-C-03 + TA-C-07 |
| BA-BP-04 复盘沉淀 | AA-AG-05 | AA-SK-05 | AA-MCP-01 | DA-T-09/10 申请写、DA-T-12 写 | TA-C-03 |
| 全链路观测与评估 | 全体 | 全体 | 全体 | Trace/Metrics | TA-C-07 |
| 阈值治理与注册 | — | 全体 | 全体 | DA-T-11 | TA-C-05 |

**校验结论**：每个 BA 流程节点有且仅有一个 Agent 承接（满足 [01 §9](./01-业务架构BA.md#9-向下游架构的交接声明) 约束）；每个 Skill 的依赖工具、每个数据对象的读写方均无悬空引用。

### 1.1 工程验证维度追溯（v1.1 新增）

业务规则 → DDD 不变量 → BDD 场景 → 测试层的完整链条见 [06 §4](./06-BDD-TDD工程验证体系.md#4-场景规则测试追溯矩阵)；业务闭环的行为化证据由 SC-01～SC-11 十一个场景覆盖：闭环五阶段（信号聚合→根因定位→处置执行→核验审计→知识沉淀）均有对应场景（SC-04与SC-11/SC-02/SC-01、SC-07与SC-10/SC-08/SC-05），审批回滚与时效专项由 SC-02/SC-03/SC-09 承载。交付维度（v1.2 新增）：每个 SC 场景均由至少一个 User Story 承接（[07 §3](./07-敏捷拆分与API契约.md#3-user-story-目录)），每个接口均有 OpenAPI 3.0 Schema 与 [08](./08-数据模型与数据字典.md) 字段级字典双重定义，形成"能力→场景→故事→接口→字段"五环追溯。

---

## 2. 需求基线覆盖核对（对照需求原文）

| 需求基线要求 | 状态 | 落点证据 |
|---|---|---|
| ≥3 个职能 Agent，身份清晰 | ✅ | 5 Agent，[02 §3](./02-应用架构AA.md#3-agent-identity-清单标准字段全量) 标准字段全量 |
| 以 AgentTeams 为协同设计基点 | ✅ | [02 §1](./02-应用架构AA.md#1-应用总体视图) 框架能力映射表 + [04 §4](./04-技术架构TA.md#4-agentteams-集成设计ta-c-01) |
| Agent Identity 清单 | ✅ | [02 §3](./02-应用架构AA.md#3-agent-identity-清单标准字段全量) |
| 闭环①任务输入 | ✅ | AA-CL-01：RocketMQ 告警投递立案 |
| 闭环②任务拆解 | ✅ | AA-CL-02：Manager 状态机拆解 |
| 闭环③上下文传递 | ✅ | AA-CL-03：Matrix 房间 + DA-T-03 共享状态 |
| 闭环④工具调用 | ✅ | AA-CL-04：Skill→MCP 两级调用链 |
| 闭环⑤结果验证 | ✅ | AA-CL-05：AA-SK-04 核验一致性 |
| 闭环⑥执行证据沉淀 | ✅ | AA-CL-06：DA-T-08 append-only + 房间归档 + Trace |
| 闭环⑦审批与回滚 | ✅ | AA-CL-07：[02 §7](./02-应用架构AA.md#7-审批与回滚设计aa-cl-07-展开) 状态机（驳回回滚 + 反向处置） |
| 闭环⑧经验沉淀 | ✅ | AA-CL-08：复盘→人工确认→DA-KB-01 |
| Skill 清单（9 属性全量） | ✅ | [02 §4](./02-应用架构AA.md#4-skill-清单9-属性全量) 5 Skill × 9 属性 |
| MCP 或等价集成契约 | ✅ | [02 §5](./02-应用架构AA.md#5-mcp-工具连接层)：2 MCP Server + Schema/鉴权/重试/幂等/审计/降级契约 |
| Agent 能力项（记忆/RAG/共享状态/轨迹） | ✅（4/4） | [03 §5.3](./03-数据架构DA.md#53-agent-能力项落实清单)：记忆 + RAG + 共享状态 + 轨迹可观测 |
| 可观测（推荐） | ✅ | [04 §7](./04-技术架构TA.md#7-可观测体系ta-c-07)：OTel 语义 + Trace/Log/Metrics + 离线评估 |
| 工具链说明（版本/调用方式/与 Agent 关系） | ✅ | [04 §2](./04-技术架构TA.md#2-技术选型总表) 选型表含成熟度依据与替换成本 |
| 可替换性说明 | ✅ | 选型表每个组件均有等价替代与迁移成本 |
| GitHub + Demo 地址 | ✅（工程交付） | [04 §3](./04-技术架构TA.md#3-部署拓扑) docker compose 一键部署 + [04 §8](./04-技术架构TA.md#8-模拟数据与演示环境) 剧本化演示 |
| 闭环证据可复现性（超出基线的增强项） | ✅ | [06](./06-BDD-TDD工程验证体系.md)：BDD 场景即闭环行为规格，演示剧本与场景测试同源 |
| 工程交付成熟度（超出基线的增强项） | ✅ | [07](./07-敏捷拆分与API契约.md) 敏捷拆分 + OpenAPI 3.0 统一契约；[08](./08-数据模型与数据字典.md) 字段级数据字典；体现"可运行、可验证"的交付准备度 |

---

## 3. 整体 Review 记录

### 3.1 审查方法

**第一轮（v1.0）**：对全部 6 份文档执行三类核对：①跨文档编号引用一致性（BA/AA/DA/TA 编号双向对账）；②业务域残留检查（保险域术语清除验证）；③需求原文逐条覆盖核对（§2）。

**第二轮（v1.1，DDD/BDD/TDD 注入后）**：在第一轮基础上追加四类核对：④DDD 构件一致性（上下文↔Agent↔数据表↔聚合四层对账）；⑤领域事件目录与 02 §7 状态机、RocketMQ 事件流的一致性；⑥BDD 场景对 BA 规则与 6 条 DA 不变量的覆盖完备性；⑦业务闭环五阶段是否均有场景承载（结果见 §1.1，全部覆盖）。

**第三轮（全团队多角色评审：产品经理/行业架构师/后端架构师/前端架构师/DBA/DevOps 逐角色对照需求提纲）**：发现 6 处新问题（R-07～R-12，见 §3.2），全部当场修复；重点覆盖业务规则分段完备性、部署拓扑自洽性、资源估算、并发控制与前端界面缺口。

**第四轮（演示可行性专项：核心技术栈核对 + 页面演示三端真实调用链审计）**：发现 1 处新问题（R-13），当场修复；确认核心组件零缺漏、演示链路非写死 mock。

**第五轮（交付体系注入：07 敏捷拆分与 OpenAPI 3.0 契约 + 08 数据字典）**：执行四类核对：⑧openapi.yaml 可解析性（YAML 解析通过：15 paths / 30 schemas）；⑨API-W 编号与 Story 引用对账（发现并修复 5 处编号错位）；⑩枚举字典三方一致性（08 §2 ↔ openapi CaseStatus/SseEvent ↔ 03 §9.2 事件目录，12 状态/10 事件全同）；⑪MCP 工具清单一致性（02 §5 的 6+3 工具 ↔ 07 API-M-01~09 ↔ openapi x-mcp-tool，全同）。本轮无遗留问题。

**第六轮（关联性审计 + 社区对标）**：全文档编号反向引用扫描，发现 4 个孤岛模块 + 1 个悬空术语（R-14～R-18），全部当场修复；同步检索社区同领域项目（AWS 官方反欺诈参考架构、GNN 反欺诈、多 Agent 金融系统、Agent 观测平台、合成金融数据）形成 [09 社区对标分析](./09-社区对标分析.md)，结论：主链路与行业参考架构同构，审批门控+审计链为差异化优势，行动项已回接 07 排期。

**第七轮（对标借鉴整合 + 全文档一致性同步）**：将 09 三项行动项整合入文档体系，按"规则→场景→字段→契约→故事"五环贯通：①新增 BA-BR-14（velocity 频次特征强制规则，13→14 条）；②新增 SC-11 验收场景（10→11 个，验收线 11/11）；③DA-T-04 新增 velocity_json 字段（03 §4 + 08 字段字典 + openapi RiskSignal Schema 三方同步，YAML 解析实测通过：15 paths / 30 schemas / velocity_json 在场）；④AA-SK-01 职责补 velocity 计算；⑤04 §8 合成数据发生器补 PaySim 分布参数；⑥US-E3-03 升 6 点并挂 SC-11、US-E7-03/05 验收标准更新（Story 总数 29 不变）；⑦00 计数与版本号同步（v1.4）；⑧09 行动项状态全部置为已整合。本轮无遗留问题。

**第八轮（Sprint 0 技术体系落地实测 + 实现反哺文档）**：仓库骨架与全栈部署一次性落地并逐项取证：①docker compose 10 容器全 Up（postgres/namesrv/broker/nacos/web-api 五个 healthcheck 全 healthy）；②`/api/health` 返回 UP 且 POST `/api/alerts` 立案落库（CASE-20260812-42f022，US-E1-01 验收达成）；③两个 MCP Server streamable-http 握手 200（protocolVersion 2025-03-26）；④合成数据冒烟档入库（500 账户/5261 交易/2 团伙）且 fn_related_graph 2 跳查询实测返回；⑤case-events Topic mqadmin 预建成功；⑥backup.sh 备份演练产出 dump；⑦AgentTeams 官方脚本（WSL2 非交互模式）安装成功，Manager/Matrix/MinIO/内置 Higress 全就绪并已接入 tradeguard-net（US-E1-02 基础设施部分达成，5 Worker 创建待真实 LLM Key 后在控制台完成）。发现并修复 2 处文档与实现偏差（R-19/R-20，见 §3.2）；部署实测坑位（RocketMQ 外挂配置 NPE、Nacos v3 强制 AUTH_TOKEN、mcp SDK 1.2.1 不支持 streamable-http 升 1.9.4、分区表授权）已回写 04 §3 备注与 scripts 文档。US-E1-03（Nacos 注册元数据/阈值热更）为 Sprint 1 事项。**架构设计落地补充（同日）**：三端按设计植入架构构件并取证——后端状态机（12 态/16 事件/18 迁移路径，与 DDL CHECK/openapi 枚举逐字一致）+ 仓储/端口适配器/乐观锁写路径模板，API-W-01~15 全量声明；存储端 04-invariants.sql 状态迁移白名单触发器与知识发布人工门控（双守护实测：非法迁移应用层 409 E-BAD-TRANSITION 与 DB 触发器 RAISE 均拒绝）；MCP 工具全集 API-M-01~09 实测可列（补 API-M-05/06）；前端全量契约声明 + 角色守卫，vite 构建通过；状态机全链冒烟：立案→聚合→调查→复核确认→审批批准（version 0→2）+ 审计链 3 条 + SSE 首帧连通。

**第九轮（实现闭环全量复核 + 契约漂移修复）**：以"路径依赖、调用闭环、文档-代码对齐"为纲执行全仓审计，发现并修复 6 类问题（R-23～R-28，见 §3.2）：①闭环第一跳接通——CaseRegistered 原无消费者（立案→聚合靠人工推动），新增 EventWorker（DB 轮询主力 / RocketMQ 尽力而为，case_id 单飞锁），rocketmq-client-python 0.5.0rc2 实测接通 case-events topic，事件 envelope 透传 trace_id；②状态机/处置/核验修复——DISPOSING 死胡同疏通（迁移 19→21，新增 DispositionFailed 出口与 RollbackEscalated 拆事件）、处置"先转状态后执行"+重试分类（幂等冲突按成功处理）、核验三分支（执行一致绝不回滚，trace 缺失仅告警；不一致走反向处置；无凭证升级人工）、_decide 条件 UPDATE 消除 TOCTOU；③mcp-core 门控加固——approval_ref 验真（含逆动作对授权："批准冻结即含解冻纠错授权"）、高风险 release 豁免仅限 ROLLBACK 态、中风险段 E-DISP-SCOPE 拒绝、建单存在性校验、70 分线经 sys_config 与 web-api 同源；④SC-06 阈值真接线——聚合/处置常量全部读配置快照（纯函数关键字缺省参兼容），Nacos 写回顺序修正（先 Nacos 后 DB），nacos_register.py 发放前读现值仅补缺键；⑤前后端契约对齐——web-portal 五页字段对齐（risk_flag 恒真修复、流水线按钮接线、审批门户展示 requested_action/escalated_at）、新增 API-W-21/22、openapi.yaml 22 路径校验通过、demo_playbook 契约迁移后 3/3 连续两轮通过（24 步断言）；⑥工程卫生——db/init/06 幂等迁移（种子键/2027 分区/触发器双写）、DB actor 守卫（trg_case_actor_gate 五对 human-only）、KPI 判定按全量/演示口径分列独立判定、data-generator 爆发簇改近 1 小时（velocity 窗口可实证触发）。修复取证：测试 169/169 全绿、KPI 报告按分列口径重生成、npm run build 通过。

**第十轮（行业术语对齐 + 核心技术栈连通复核）**：以"术语是否符合金融风控行业语境、核心技术栈是否真实参与业务架构、跨系统数据传输是否真实打通"为纲复核，发现并修复 3 处问题（R-29～R-31，见 §3.2）：①术语对齐——对前端全部展示文案做行业术语审计，risk_case 统一「案件」（与立案/结案自洽）、approval_record 改「审批单」（弃 ITSM「工单」）、kb_document 改「知识条目」（弃「文档」）、disposition_record 改「处置记录」（「凭证」系会计用语）、「事件」仅指领域事件，12 态标签/流程指引/事件名/错误提示全量改写（讲人话且专业，避免想当然填充，也不低幼化）；②Skills 注册元数据一致性——nacos_register.py 中 AA-SK-05 的 kernel 指向不存在的 retrospective.py，修正为 knowledge.py 并补 kernel_note（复盘入库申请入口实为 verification.py::VerificationService._retrospective），与 SKILL-DISPATCH.md 同源；③可观测数据流接通——AgentScope Studio 原已部署但零数据流入（仅容器在跑、观测面与业务面割裂），新增 `app/core/tracing.py` OTLP/HTTP 直推（best-effort 双通道：JSONL 留痕 + OTLP 上报 Studio），并按 Studio v1.0.9 源码实证的混合字段口径适配（顶层 resourceSpans camelCase / 嵌套 span 字段与 attribute 值 snake_case，标准全 camelCase OTLP JSON 会被静默丢弃），案件编号经 md5 映射 traceId + 写 gen_ai.conversation.id 实现 Studio 按案件分组会话；演示剧本回放后 Studio SQLite 实测落库技能 span 并按案件分组。修复取证：测试 169/169 全绿、演示剧本 3/3（24 步断言）、npm run build 通过、Studio span_table 实测有数据。

**第十一轮（Higress 网关接通，真实承载流量）**：以"核心技术栈必须真实参与业务流量、不做部署摆设"为纲，对 Higress（TA-C-04）落地实况攻坚修复（R-32，见 §3.2）：①端口口径修正——all-in-one 2.2.3 数据面 HTTP 实测监听容器内 8080（`GATEWAY_HTTP_PORT` 缺省），compose 旧映射 `8180:80` 恒不通，改 `8180:8080`；②路由下发——控制台首次初始化未完成（REST API 401）的情况下改经容器 /data 文件仓下发：McpBridge 将 web-api/mcp-core/mcp-external-mock 注册为 dns 型服务源（dns 型要求带点域名，compose 为三服务配置 `*.tg.local` 网络别名经 Docker 内嵌 DNS 解析），Ingress `/api` 经 `higress.io/destination: web-api.dns:8000` 指向 web-api；③流量切换——web-portal nginx 上游由 `web-api:8000` 改指 `higress:8080`，门户全部 /api 业务请求与 SSE 真实过网关（envoy 集群计数 upstream_rq_200 随请求递增、经网关 /api/cases 返回真实库数据取证）；④场景化取舍（不为用而用、避免孤岛耦合）——web-api→MCP 内核通道保持直连：处置执行是状态机关键路径，不将网关单点耦合进处置可用性，且该链路为容器内东西向调用无外部入口治理需求；三个 MCP 服务已全量注册为网关服务源，生产形态按需加路由即开；⑤可复现——新增 `scripts/higress_routes.py` 幂等重建路由（写文件仓→重启网关→探活 :8180/api/health=200），应对 `down -v` 清卷场景。如实保留项：控制台 UI（:8001）首次初始化（设管理员密码）未完成，路由经文件仓下发而非控制台，不伪造。修复取证：网关重建后演示剧本复跑 3/3（24 步断言）、门户 /api 与 SSE 经网关正常、npm run build 通过。

**第十二轮（AgentTeams 协同执行真接线 + 五维映射落地）**：以"核心协同框架（TA-C-01）的五维协同（角色编排/任务拆解/上下文传递/协同执行/状态追踪）是否真实映射到框架能力、Worker 是否真能调到业务工具"为纲核查，发现并修复 1 处问题（R-33，见 §3.2）：①运行态复原——Docker Desktop 重启后 controller 因 docker.sock bind-mount 瞬时失败 Exited(127) 且 unless-stopped 不自愈、4 Worker 停 Sleeping，docker start 拉起 controller、`agt worker wake` 唤醒全部 Worker（Manager=default + aa-ag-02~05 全 Running，model=qwen3.8-max / runtime=copaw）；②组网复原——Worker sleep/wake 重建容器丢失手工接入的 tradeguard-net（解析不到 mcp-core），重新 connect controller/manager/4 worker；③协同执行接线——发现 4 Worker 的 mcporter 工具桥均"No MCP servers configured"（协同执行链路断裂），逐 Worker 注入 tg-core(mcp-core:8101)/tg-external(mcp-external-mock:8102) 两个 streamable-http MCP Server，`mcporter list` 全 healthy（12+3 工具在场），并实测 Worker aa-ag-02 经工具桥调用 `tg-core.query_transactions` 返回真实库内交易行（Worker→MCP→业务库端到端打通）；④五维映射落地——04 §4 将"协同映射声明"一行扩为五维协同→框架能力对照表（逐项附实测证据），并如实声明集成深度（独立部署栈、演示剧本与 Worker 同源技能内核共用状态机/MCP/审计底座、无分叉实现）；⑤可复现——Worker 重建会丢手工组网与容器层 mcporter 配置，固化 `scripts/agentteams_doctor.py` 幂等体检恢复（拉起 controller→唤醒 Worker→接 tradeguard-net→注入 MCP 桥→校验 12+3 工具），实测一次跑通全绿。修复取证：`agt get managers/workers` 全 Running、4 Worker mcporter 12+3 healthy、端到端工具调用返回真实数据、doctor 脚本幂等复跑 OK。另按"业务场景驱动、拒绝孤岛"原则完成百炼 MCP 广场适配器评估并决策**不做**（广场无持牌征信/反诈舆情/客服投诉类服务，场景不匹配 + 征信合规边界 + 演示确定性基线；AA-MCP-02 端口/防腐层已锁定未来真实适配器零改动接入的价值），评估结论留痕于 02 §5 与 09 速览表。

**第十三轮（一键启动脚本 + 真实启动取证，拒绝可用性幻觉）**：以"系统到底能不能用、数据能不能通，必须由实际启动情况验证而非文档宣称"为纲，新增 `scripts/start_all.py` 一键启动 + 数据通路自证脚本（R-34，见 §3.2）。脚本九步：引擎预检（不可达自动拉起 Docker Desktop）→ compose down（保留数据卷）→ 逐端口清障 → compose up → 逐服务真实探活（不看容器状态，只看真实响应）→ 数据就位（空库自动重灌 + 阈值播种）→ Higress 路由重建 → AgentTeams 体检恢复 → C1~C9 + X1 数据通路硬证据（真实立案 202 → EventWorker 无人工干预自动推进 DISPOSED → 审计含自动通道准入 + 处置凭证 release/executed → AA-SK-01 span 落库）。实测首跑即以真实启动暴露 2 处真问题并修复：①Windows Docker Desktop 宿主端口监听者是引擎进程 com.docker.backend.exe，taskkill 清端口会把整个引擎杀掉（首跑实证引擎消失）——固化为清障铁律：先 tasklist 识别进程映像，Docker 进程一律不杀，改停归属容器或等待自然释放（docker ps 归属解析兼容 0.0.0.0: 与 127.0.0.1: 两种发布形态）；②冒烟立案 source_type 取值在契约枚举外被 422 拒收（AlertIn pattern 守门实证有效）——改用枚举内 demo_script。修复后复跑 20/20 全绿 exit 0（含网关转发、门户经网关、Nacos 快照、AgentTeams 4 Worker Running）。

**第十四轮（文档×代码×展示 HTML 三端一致对齐，最齐全实现为准）**：以"三端不得有孤岛功能/接口、不得有空白未落地宣称、任何一端缺失的能力以最齐全一端为准补齐而非降格宣称"为纲，对 docs/00~09、services/web-api+mcp-core+web-portal、docs/reports/tradeguard-overview.html（21 页展示材料）做逐条反向核实（约 130 条陈述），发现并修复约 30 处不一致（R-35，见 §3.2）。要点：①后端补齐——`db/init/03-umodel-fallback.sql` v_graph_edge 补第四类边 SAME_CONTACT（主体档案 contact_hash，对齐 03 §3 四类边宣称，mcp-core query_related_graph 泛化透传零改动）；investigation 补 `_cfg_int` 热值读取，BR-06 加分改经 `br-06-fraud-link-bonus` 键（01-schema 种子 + nacos_register THRESHOLDS 同步），01 §5「7 条规则 Nacos 热更新」由宣称变事实；`scripts/nacos_register.py` AA-MCP-01 工具元数据改为在码真实 12 工具全集（原列表含 update_case_context/publish_kb_document 等未编号工具名，属失真）；②前端补齐——审批/审计/知识库三页接入 SSE 事件驱动刷新（「全页面 SSE」成立）；案件工作台新增处置凭证区块（API-W-22 有可视化消费方）与审计 trace_id 展示；可观测面板新增动态阈值配置区块（API-W-16 不再孤岛，暴露 source/updated_at）；角色切换移入顶栏；labels.js 补 SAME_CONTACT/DISP_STATUS_META/THRESHOLD_LABELS；③展示 HTML 修正约 20 处不实陈述（compose 9→12 服务、20 万→10 万行、mcp-core 12 真实工具名单、双守护口径、阶段 D 名称、KPI 全量口径 01/03/04/05 未达标仅 02 达标、真问题 3→2 处、状态机图事件/状态口径等），并修复翻页 bug（.cover display:flex 覆盖 .slide display:none 致 21 页翻页恒为最后一页）；④文档回写——01 §2.2 阶段字母对齐 §4.1（E→K、D→D/H）、AA-SK-04 技能定义补分支 3（RollbackEscalated 诚实语义）。取证：pytest 169 例全量回归、npm run build、运行时视图/阈值对齐见 07 Sprint 13 执行记录。

**第十五轮（全文档表述专业化审计）**：以"对外材料不得保留外部活动相关的直白字样，一切以工程与业务术语表述"为纲，对 docs/00~09、skills/、services/、scripts/、README/CLAUDE.md 与展示 HTML 全局扫描（24 文件、120+ 处命中），逐处语义替换（R-36，见 §3.2）：①阶段类字样统一为设计阶段/实现 M1~M3/验收；②来源类字样统一为需求基线/需求原文；③强制性字样统一为核心基座/核心技术栈/架构基点；④评审对象类字样移除或转为客观风险表述；⑤改动标题（01 §2.2、02 §3/§4/§6、03 §5.3、04 §4.1、05 §2）的锚点跨文档链接全部同步更新。语义、证据与编号体系不变；kpi_report.py 输出文案与 kpi-report.md 同源一致；文档体系升版 v1.4.10。

### 3.2 发现并已修复的问题

| # | 问题 | 位置 | 修复 |
|---|---|---|---|
| R-01 | 00 编号示例残留保险域（BA-CAP-02 理赔处置、BA-BP-01 理赔全流程、BA-BR-01 自动赔付阈值、BA-KPI-01 结案时效） | 00 §3 | 已改为交易风控域示例（风控处置/交易风控全流程/自动处置边界/事件响应时效） |
| R-02 | AA-AG-01 失败处理错误引用 BA-BR-08（该规则是处置核验超时，非 Worker 调度超时） | 02 §3.1 | 改为引用 BA-KPI-01 响应时效监控 |
| R-03 | 02 章节号错位（Identity 清单标题为 §2，应为 §3 以匹配 00 引用表） | 02 | 已修正为 §3 |
| R-04 | SC-02 误引用 DA-INV-03（幂等不变量与审批门控无关） | 06 §2 | 改为 DA-INV-02 + AA-SK-03 安全边界 |
| R-05 | 领域事件数量表述不一致（8 行目录实含 10 种事件类型） | 06 §1/§3 | 统一为"8 组领域事件（10 种事件类型）" |
| R-06 | 01 §10 mermaid 错别字"幵放"及保险域残留词"拒赔" | 01 §10 | 已改为"开放"与"冻结/拦截" |
| R-07 | 【产品】风险分 40–69 中风险分段规则缺口：调查后"排除欺诈"事件若风险分仍≥40，无规则定义去向，与自动处置边界矛盾 | 01 §4.1/§5 | BA-BR-01 补充中风险分段（转人工复核），主流程图增加 E1/J2 分支，新增 SC-10 |
| R-08 | 【后端/行业】审批无时效约束：高风险事件审批人可能长时间不在线，事件无限期滞留 | 01 §5、04 §10 | 新增 BA-BR-13（审批时效 30 分钟超时升级）+ SC-09 + 审批门户超时标红 |
| R-09 | 【后端/DevOps】部署拓扑自相矛盾：图中 MCP 流量旁路 Higress，与 §5"统一入口无旁路"声明冲突；AgentTeams 既是"官方脚本安装"又列入 compose 服务清单 | 04 §3 | MCP 调用改经 Higress；AgentTeams 改为独立部署（非 compose 服务），补充服务依赖顺序 |
| R-10 | 【DevOps】资源估算不足：JVM 组件（RocketMQ/Nacos）+ PolarDB + Higress + AgentTeams 内存合计约 10–12 GB，4C8G 无法承载全栈；无备份策略 | 04 §3/§9 | 最低资源改为 8C16G（4C8G 仅裁剪部署）；新增 backup.sh 备份与恢复策略 |
| R-11 | 【DBA】risk_case 状态并发写入无冲突控制；approval_record 工单创建方缺失（仅写"人类审批官写"，工单谁创建未定义） | 03 §4/§6 | DA-T-03 增加 version 乐观锁；DA-T-07 明确 AG-04 创建工单、人类回填 decision，读写矩阵同步 |
| R-12 | 【前端】人机界面仅在拓扑图出现，无页面/角色/技术选型设计，BA §6 四类角色的交互载体无落地 | 04 §10（新增） | 新增人机界面设计节：4 页面 × 4 角色映射，Vue 3 + Element Plus，API 复用 mcp-core |
| R-13 | 【演示可行性】"后端 API 由 mcp-core 同源提供 REST"不成立：MCP Server 非 REST 端点，浏览器无法直连，缺真实 web-api 后端层；人类审批写路径与前端实时推送未落链路 | 04 §3/§10、02 §7、03 §9.2 | web-portal 改为前端+web-api(FastAPI) 双进程；新增 §10.1 三端真实调用链（接口清单/SSE 推送/三条真实性硬约束）；审批写路径与 Approval* 事件代发方同步落定 |
| R-14 | 【孤岛】TA-C-02 云 Skills 门户仅在选型表一行，无任何使用设计，而需求明确要求说明"鉴权、编排、端到端体验" | 04 §4 | 新增 04 §4.1 云 Skills 门户使用设计（使用范围/鉴权/编排/无云账号时降级四维度） |
| R-15 | 【孤岛】DA-T-12 agent_memory 有表有权限但无读写时机，任何 Agent 都不消费它，记忆形同虚设 | 03 §4、02 §4 | DA-T-12 补写时机（阶段结束写摘要）与读时机（SK-02/SK-05 检索）；AA-SK-02 依赖工具增加同主体历史记忆检索 |
| R-16 | 【孤岛】DA-KB-02 监管规范库声明消费方 AA-SK-04，但 SK-04 依赖工具清单无 KB-02，悬空引用 | 02 §4、03 §5.1 | AA-SK-04 依赖工具补 DA-KB-02 审计检查清单检索 |
| R-17 | 【孤岛】BA-KPI-05 留痕完整率无度量路径：04 §7 评估脚本仅覆盖 KPI-02/03/04 | 04 §7 | 补审计完整性校验任务：按日遍历 DA-T-06 处置记录对账 DA-T-08 完整记录比例 |
| R-18 | 【悬空术语】"争议/拒付（Chargeback）"定义于术语表但无任何业务功能承接 | 01 §2 | 术语定义回接业务入口：持卡人否认交易信号由投诉记录源接入（BA-BP-02） |
| R-19 | 【文档与实现偏差】04 §3 部署形态写"web-portal 同一容器双进程"与"umodel compose 服务"，Sprint 0 实际实现为 web-portal/web-api 双容器拆分、UnifiedModel 走 SQL 视图退化路径 | 04 §3 | 部署形态条目改写与实现一致（拆分理由：职责隔离/独立伸缩）；UnifiedModel 退化指向 db/init/03-umodel-fallback.sql 与 TA-C-08 替换成本列；新增 Windows 部署实测备注（RocketMQ/Nacos 坑位） |
| R-20 | 【文档与实现偏差】03 §4 DA-T-01 account 读写方未标合成数据发生器，而数据发生器需写 account（含团伙打标 UPDATE），权限矩阵落地时缺授权导致冒烟失败 | 03 §4、db/init/02-roles.sql | DA-T-01 读写方补 SIM 写（含 UPDATE）；02-roles.sql 补 GRANT INSERT, UPDATE ON account TO tg_app |
| R-21 | 【实现先行未回写】Sprint 0 架构落地时状态机引入 7 个内部迁移事件与 case_state_transition 白名单表，未先回写 03 §9.2 目录与 08 字典（违反 03 §9.4"先改目录再改代码"纪律） | 03 §9.2、08 §3 | 已补回写：03 §9.2 增"领域事件 vs 状态机内部迁移事件"分类声明；08 新增 DA-T-13 字典条目（表数 12→13，治理辅助表不计入业务聚合）；后续严格执行目录先行 |
| R-22 | 【测试欠债】状态机与提前模板化端点（W-07/W-09/W-12/13）在 Sprint 0 先行落地，但 06 §3 要求的配套单测未同步产出（违反先测后码原则） | 06 §3、US-E5-06 | 列为 Sprint 1 首批事项：状态机迁移/actor 守卫/乐观锁单测先行补齐，并作为 E5 各 Story 的前置门禁 |
| R-23 | 【闭环断裂】CaseRegistered 无任何消费者，闭环第一跳（立案→聚合）靠人工推动；RocketMQ 客户端缺失致发布器恒降级进程内队列，"事件驱动"宣称不实 | 02 AA-CL-01/02、04 §10.1 | 新增 EventWorker（DB 轮询主力 2s + MQ 尽力而为，case_id 单飞锁与 /aggregate 共用）；rocketmq-client-python 0.5.0rc2 实测接通（case-events 4 读写队列有消息）；rocketmq-init 一次性建 topic；事件 envelope 透传 trace_id；04 §10.1 如实声明降级策略 |
| R-24 | 【状态机/核验缺陷】DISPOSING 失败无出口成死胡同；verification 在"执行一致但 trace 不全"时错误回滚、反向失败时抛异常卡死案件；_decide SELECT→UPDATE 无锁（TOCTOU） | 03 §9.2、app/skills | 21 迁移（+DispositionFailed 出口、+RollbackEscalated 拆事件）；核验三分支（一致绝不回滚/不一致反向处置/无凭证升级 MANUAL_REVIEW）；verification.p0 basis 条件化；条件 UPDATE 终审（0 行→409 E-ALREADY-DECIDED） |
| R-25 | 【门控纸糊】mcp-core approval_ref 从不校验；高风险无凭证 release 豁免可被滥用；中风险段与 web 层守卫失配 | services/mcp-core | approval_ref 验真（decision/case 匹配 + requested_action 匹配或逆动作对，DA-INV-03）；release 豁免仅限 ROLLBACK 态；40≤score<70 E-DISP-SCOPE；建单存在性 E-NOT-FOUND；70 分线经 sys_config 同源 |
| R-26 | 【配置空转】SC-06 阈值热更新：播种键全为死键、聚合/处置阈值硬编码、70 分线双端各自硬编码，热更宣称不成立 | 04 §6、web-api/mcp-core | 全部阈值改读 config.snapshot()（纯函数关键字缺省参兼容）；mcp-core 70 线查 sys_config（DB 镜像作 Nacos 降级源）；PUT 写回顺序修正（先 Nacos 后 DB）；nacos_register.py 仅补缺键不覆盖 |
| R-27 | 【契约漂移】web-portal 五页字段与实况不符、risk_flag 恒真 bug、流水线函数零引用；demo_playbook 旧契约跑不通 | web-portal、openapi、scripts | 字段逐项对齐（信号/证据/审计/SSE/trace/审批）；流水线按钮按状态接线（API-W-21/22 新增）；openapi 22 路径校验；playbook 契约迁移（202/severity 枚举/decide/review/分页）3/3 连续两轮 |
| R-28 | 【工程卫生】check_pg 端口错（5432→5433）、KPI 判定以 demo 达标掩盖全量未达标、孤儿 logs/traces.jsonl、data-generator 爆发簇 30~55 天前致 velocity 窗口恒空、运行中 DB 卷与 init SQL 漂移 | scripts、db/init | 逐处修复：PGPORT 缺省读取、KPI 按全量/演示口径分列独立判定、孤儿文件删除、爆发簇改近 1 小时、06-closedloop-fix.sql 幂等迁移 + 01/04 双写一致 |
| R-29 | 【术语漂移】前端展示文案含非金融风控行业语境术语（approval_record 用「工单」、kb_document 用「文档」、disposition_record 用「凭证」），属想当然填充而非行业用语 | web-portal/src/labels.js、各 views、web-api 用户态文案 | 全量对齐行业术语：案件（立案/结案自洽）/审批单（金融审批语境）/知识条目/处置记录/「事件」仅指领域事件；12 态标签、流程指引、21 事件名、错误提示、后端用户态报错同步改写；术语口径写入 labels.js 头注释 |
| R-30 | 【注册元数据失配】nacos_register.py 中 AA-SK-05 的 kernel 指向不存在的 retrospective.py，与 SKILL-DISPATCH.md 及真实代码不符（Skills Registry 宣称失真） | scripts/nacos_register.py | kernel 改 services/web-api/app/skills/knowledge.py 并补 kernel_note（复盘入库申请入口为 verification.py::VerificationService._retrospective）；重注册并经 Nacos 读回核账一致 |
| R-31 | 【观测数据流断裂】AgentScope Studio（TA-C-07）已部署但零数据流入——容器在跑、观测面与业务面割裂，「可观测」宣称不实 | 04 §7、app/core/tracing.py | 新增 OTLP/HTTP 直推（best-effort：JSONL 留痕 + OTLP 上报，断链仅降级不阻断处置面）；按 Studio v1.0.9 源码实证混合字段口径适配 _to_otlp()；case_id→traceId md5 映射 + gen_ai.conversation.id 实现按案件分组；演示回放后 Studio SQLite 实测落库技能 span；Observability 外链文案如实标注连通状态 |
| R-32 | 【网关旁路】Higress（TA-C-04）已部署但未真实承载流量：compose 映射 8180:80 恒不通（数据面实际监听 8080）、控制台 REST API 401 无法注入路由、04 §5 如实声明旁路直连 | docker-compose.yml、web-portal/nginx.conf、higress /data 文件仓、04 §5 | 端口改 8180:8080；经文件仓下发 McpBridge（dns 型服务源 + *.tg.local 网络别名，Docker 内嵌 DNS 解析）+ Ingress /api→web-api.dns:8000；nginx 上游改 higress:8080，门户全部 /api 与 SSE 真实过网关（envoy upstream_rq_200 计数取证）；新增 scripts/higress_routes.py 幂等重建（down -v 清卷可复现）；web-api→MCP 保留直连为场景化取舍（处置关键路径不耦合网关单点）并如实声明 |
| R-33 | 【协同执行断链】AgentTeams（TA-C-01）Worker 未接业务工具：Docker Desktop 重启后 controller Exited(127) 不自愈、4 Worker 停 Sleeping；唤醒重建后又丢 tradeguard-net 组网，且 4 Worker 的 mcporter 工具桥均"No MCP servers configured"，"协同执行"维度映射不实 | scripts/agentteams_doctor.py（新增）、04 §4、02 §1 | docker start controller + agt worker wake 唤醒 4 Worker（Manager/Worker 全 Running，qwen3.8-max）；重连 tradeguard-net；逐 Worker 注入 tg-core(12)/tg-external(3) 两个 MCP Server，mcporter list 全 healthy 并实测 Worker 调 query_transactions 返回真实库内交易行；04 §4 协同映射声明扩为五维协同→框架能力对照表（附实测证据）+ 集成深度如实声明；doctor 脚本幂等体检恢复固化可复现 |
| R-34 | 【可用性幻觉】系统是否可用、数据是否通路无一键自证手段：端口占用/引擎未起/卷漂移/路由丢失等任一环节都可能"看起来在跑"实则不通；且首跑实证两处真坑——Windows Docker Desktop 宿主端口监听者是引擎进程 com.docker.backend.exe（taskkill 清端口会杀掉整个引擎）、冒烟立案 source_type 契约枚举外被 422 拒收 | scripts/start_all.py（新增）、CLAUDE.md 已知环境实况 | 一键启动脚本九步（引擎预检自动拉起 Docker Desktop→down 保卷→逐端口清障→up→真实探活→数据就位→Higress 重建→AgentTeams 体检→C1~C9+X1 数据通路硬证据）；清障铁律：tasklist 识别进程映像，Docker 进程不杀改停归属容器/等待释放，docker ps 归属解析兼容 0.0.0.0: 与 127.0.0.1: 发布形态；冒烟立案改枚举内 demo_script；复跑 20/20 全绿 exit 0（真实立案→EventWorker 自动 DISPOSED→审计/凭证/span 落库取证） |
| R-35 | 【三端不一致】文档×代码×展示 HTML 逐条反向核实发现约 30 处失配：图谱视图缺第四类边 SAME_CONTACT（03 §3 宣称四类边）；BR-06 加分硬编码（01 §5 宣称 Nacos 热更）；nacos_register AA-MCP-01 工具列表含未编号工具名；审批/审计/知识库三页无 SSE（「全页面 SSE」不实）；API-W-16/W-22 无门户可视化消费方（孤岛接口）；角色切换在侧栏（HTML 宣称顶栏）；展示 HTML 约 20 处不实陈述（9→12 服务、20 万→10 万行、假工具名、双守护口径、KPI 全量口径、真问题 3→2、状态机图事件/状态混淆等）+ 翻页 bug | db/init/03-umodel-fallback.sql、app/skills/investigation.py、app/main.py、db/init/01-schema.sql、scripts/nacos_register.py、web-portal 五页+labels.js+App.vue、docs/reports/tradeguard-overview.html、docs/01 §2.2、skills/AA-SK-04 | 以最齐全一端为准补齐而非降格：视图补 SAME_CONTACT 边（contact_hash 自连接）；investigation 补 _cfg_int 热读 + br-06-fraud-link-bonus 键三处播种（schema 种子/Nacos/缺省常量回落）；工具元数据改在码 12 工具全集；五页全接 SSE 事件驱动防抖刷新；工作台补处置凭证区块 + 审计 trace_id；可观测补阈值区块（source/updated_at）；角色切换入顶栏；HTML 约 20 处修正 + 翻页 bug 修复（.slide.active.cover）；文档阶段字母与技能三分支回写；取证见 07 Sprint 13 |
| R-36 | 【表述专业化】全仓残留外部活动相关直白字样（活动阶段/需求出处/评审对象等措辞，24 文件 120+ 处），与"专业工程交付物"定位不符 | docs/00~09、skills/README + SKILL-DISPATCH、services（mcp-core README、test_scenario_matrix 文档串）、scripts（kpi_report/demo_playbook/rocketmq-init）、README.md、CLAUDE.md、.env.example、docker-compose.yml、db/init/03 注释、docs/reports（HTML + kpi-report.md） | 逐处语义替换：阶段类→设计/实现/验收；来源类→需求基线/需求原文；强制类→核心基座/核心技术栈/架构基点；评审对象类→移除或转客观表述；标题锚点变更处（01 §2.2、02 §3/§4/§6、03 §5.3、04 §4.1、05 §2）跨文档链接同步更新；kpi_report.py 输出与 kpi-report.md 同源；00 升版 v1.4.10 + 07 Sprint 13 补充记录 |

### 3.3 核对通过项（无问题）

- 全部 DA-T / DA-N / DA-L / DA-KB 编号引用均有定义，无悬空；
- 全部 TA-C 编号（01–10）在 [04 §2](./04-技术架构TA.md#2-技术选型总表) 有定义，被引用处语义一致；
- 业务域残留扫描：仅 2 处"理赔"字样，均为业务领域描述（00 业务领域、01 场景映射），合法保留；
- BA 规则 → 配置位置的映射闭环：阈值类规则（BR-01/02/05/06/08）→ Nacos 配置（TA-C-05）→ DA-T-11 快照，链路完整；
- 人机边界在三处一致声明：[01 §6](./01-业务架构BA.md#6-业务角色与职责)、[02 §3.3/§4 AA-SK-03](./02-应用架构AA.md)、[03 §6](./03-数据架构DA.md#6-agent-数据读写矩阵)（发布权只在人类）；
- **v1.1 新增**：DDD 四层对账通过（7 上下文 ↔ 5 Agent ↔ DA 表 ↔ 5 聚合无孤立构件）；领域事件发布/订阅方与 02 §3 Agent 职责一一对应；SC 场景引用的规则/不变量/闭环编号全部存在；06 与 03 §9 的锚点链接全部可达。

### 3.4 残余风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| UnifiedModel 实际 DSL 语法与设计示意存在出入 | 中 | 03 §3.3 已声明"示意，以官方规范为准"；TA 表给出退化为 SQL 视图的兜底（约 2 人日） |
| 模拟征信/舆情的真实性被外部质疑 | 低 | [01 §8](./01-业务架构BA.md#8-数据可得性与合规声明个人数据边界) 已正面声明可得性边界与生产替换路径 |
| AgentTeams Worker 自定义能力边界待实测验证 | 中 | 实现阶段第一个里程碑即跑通"Manager 分派→Worker 调 MCP"最小闭环，失败则回退官方示例形态 |
| KPI 目标值（召回 ≥85% 等）需演示数据集支撑 | 中 | 合成数据发生器按四类欺诈模式可控注入（04 §8），评估脚本固化于仓库保证可复现 |

---

## 4. 评审结论

1. **完整性**：4A 四视角 + 总则 + 本报告 + 工程验证体系 + 敏捷/API + 数据字典 + 社区对标共 10 份文档，构成单一系统的完整阐述，无孤立文档；
2. **一致性**：十五轮交叉核对后，编号引用、术语、人机边界、规则-配置映射、DDD 构件四层对账、部署拓扑自洽性全部一致；第六轮编号反向引用扫描确认无孤岛模块（R-14～R-18 已修复）；第七轮对标借鉴整合后规则/场景/字段/契约/故事五方计数全部同步（14 规则/11 场景/13 表/15 paths/29 Story）；第九轮实现复核再同步：状态机 21 迁移、API-W 22 paths、API-M 15 项（12 在码核心工具 + 3 外部模拟）、测试 169 例、演示剧本 3/3；第十轮行业术语与核心技术栈连通复核：前端文案全量对齐金融风控行业术语、Skills Registry 元数据与代码同源、可观测 OTLP 数据流实测接通（Studio 按案件分组落库技能 span）；第十一轮网关接通：Higress 承载门户全部业务流量（dns 型服务源 + 文件仓路由下发，envoy 计数取证），幂等重建脚本入库；第十二轮协同框架接线：AgentTeams 五维协同映射落地，4 Worker 经 mcporter 工具桥实测调到业务 MCP（端到端返回真实库数据），体检恢复脚本入库；第十三轮一键启动自证：start_all.py 以真实启动 + 端到端立案闭环取证系统可用性（20/20 全绿），并固化"Docker 引擎进程不可 taskkill"清障铁律；第十四轮三端一致对齐：文档×代码×展示 HTML 逐条反向核实约 130 条陈述，约 30 处失配以最齐全一端为准补齐（SAME_CONTACT 第四类边、BR-06 热更新接线、全页面 SSE、API-W-16/W-22 门户消费方、展示 HTML 约 20 处修正与翻页 bug），消除孤岛接口与空白宣称；第十五轮表述专业化：全仓外部活动相关直白字样替换为工程与业务术语（24 文件 120+ 处，标题锚点与跨文档链接同步），语义与证据不变；
3. **需求符合性**：核心要求（AgentTeams、Skill、≥3 Agent、Identity 清单、8 项闭环、GitHub+Demo 规划）100% 覆盖；推荐项（MCP、Nacos、Higress、PolarDB、RocketMQ、UnifiedModel、可观测）按必要性全部纳入且附替换成本；
4. **闭环完备性**：业务闭环五阶段均有 Agent 承接、数据承载、技术组件支撑与 BDD 场景验证（含中风险分段与审批时效两个第三轮补充分支），四层证据链闭合；
5. **可落地性**：所有组件均有官方安装路径与成熟社区背书，版本策略保守，资源估算、备份策略、演示环境与合成数据方案明确，测试策略与分阶段 DoD 已定义。

**结论：文档集通过第十五轮整体评审（累计 36 处问题修复留痕，第十五轮完成全文档表述专业化（R-36，外部活动相关直白字样全量替换为工程与业务术语，锚点链接同步，语义与证据不变）；第十四轮完成文档×代码×展示 HTML 三端一致对齐——约 130 条陈述逐条反向核实、约 30 处失配以最齐全一端为准补齐：SAME_CONTACT 第四类边入视图、BR-06 加分纳入 SC-06 热更新、全页面 SSE 成立、API-W-16/W-22 获门户可视化消费方、展示 HTML 约 20 处不实陈述修正并修复翻页 bug，无孤岛接口/无循环依赖/无空白宣称；第十三轮以真实启动取证完成一键启动自证——scripts/start_all.py 拉起全栈并端到端验证数据通路 20/20 全绿（真实立案 202→EventWorker 无人工干预自动 DISPOSED→审计含自动通道准入 + 处置凭证 release/executed + AA-SK-01 span 落库），首跑实证并固化"Docker 引擎进程不可 taskkill"清障铁律与 source_type 契约枚举守门；第十二轮接通核心协同框架最后一环——AgentTeams 五维协同映射落地且 4 Worker 经 mcporter 工具桥实测调到业务 MCP（端到端返回真实库数据），体检恢复脚本入库；第十一轮接通 Higress 网关真实承载门户业务流量并取证：envoy 集群计数随请求递增、经网关读取真实库数据、演示剧本复跑 3/3；第十轮 3 处"术语/注册元数据/观测数据流"问题修复并取证：测试 169/169 全绿、演示剧本 3/3、KPI 按全量/演示口径分列重生成），核心技术栈零缺漏且全部真实参与业务架构（AgentTeams 协同执行、Nacos 阈值热更、OTLP 可观测、Higress 网关路由、RocketMQ 事件闭环均实测接通），页面演示三端真实调用链已实测闭合（事件驱动闭环第一跳由 EventWorker 自动承接），敏捷拆分/OpenAPI 契约/数据字典交付体系完备，且已与社区同领域参考架构对标并将借鉴项五环贯通落地（[09](./09-社区对标分析.md)），可作为交付提交基线。**

---

## 5. 后续修订纪律

任何后续修改必须：①同步更新本追溯矩阵；②受影响编号在全部文档中检索替换；③在本报告 §3.2 追加修复记录。
