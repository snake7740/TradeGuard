# 14 · 增强路线图多层分拆：4A → DDD → BDD → TDD → 敏捷排期

> 版本：v1.7 ｜ 2026-08-22 ｜ 上游：[docs/13 增强路线图](./13-行业前沿对标与能力增强路线图.md)
> **v1.2 状态：US-E8~E12 全部已实施**——13 增强点集成完毕，SC-12~17 落地，全量金字塔 296 例全绿，浏览器四角色×多菜单协作完整前端路径复测通过（立案→聚合→调查→提请→控辩→批准→执行→预检→核验归档→知识沉淀人审发布）。
> **v1.3 增补：LoopEngine 环设施（已实施）**——三层环：L1 失败归宿（processing_deadletter DA-T-16，累计重试上限驻车 + E-WORKER-DLQ + /api/deadletter 双重门控人工复位）、L2 双轮有界环（调查反思缺口 replan_from_gaps 二轮补查，上限 2 轮）、L3 慢环归因（proposal_attribution DA-T-17，规则提案发布后效果可度量）。编号接续 DA-T-16+：新增规则 BA-BR-22（环治理）、事件 25→26、表数 15→17、场景 SC-19/20（18→20）、排期 US-E13；全量回归 30 文件 312 例全绿。纪律不变：状态机仍是迁移权威（DA-INV-01/02，DLQ 不改案件状态）、人工门不被环绕过（复位须 human:*）、环记录只增。
> **v1.4 增补：RAG 深化——案例分析语料 × B 端知识助手（已实施）**——两块：① 语料面：归档复盘升级结构化案例分析（verification._retrospective 四段：案件概况/手法指纹（主型×分布）/处置结论/复用提示，标题携主型检索锚点）；② 消费面：/api/kb/ask B 端问答（API-W-27，仅引用已发布知识 doc_id 对齐、未命中声明无先例、人工角色门 + kb.ask 留痕）× AA-AG-06 知识助手 soul。编号接续：新增规则 BA-BR-23（问答治理）、场景 SC-21/22（20→22）、排期 US-E14；零新表（复用 kb_document/kb_embedding）、零新事件、零状态机变更。AgentTeams 自流水线内部 worker 扩展出 B 端服务角色（只检索引用、不裁决不处置不发布）。
> **v1.5 增补：企业资质外部源——外部集成双轨（已实施）**——调查深查源自征信/舆情/投诉三源扩至四源：新增企业资质五维（工商状态/经营异常/行政处罚/司法涉诉/关联集中度 + 合成 risk_flag，API-M-16 query_enterprise）。双轨：厂商开放平台 Key（ENTERPRISE_VENDOR_KEY/URL）在则真实轨，失败/无 Key 降级 mock 且 degraded+vendor_error 留痕；调用链路与生产一致（AA-MCP-02 既有端口，换实现不建新路）。合规分层：个人征信因《征信业务管理办法》持牌要求与 PIPL 授权红线保留 mock 不接真实源；企业资质为可合规接入点。编号接续：新增规则 BA-BR-24（外部源治理）、场景 SC-23（22→23）、排期 US-E15；零新表、零新事件、零状态机变更；输出仅线索不裁决（同 BR-16/INV-07 精神）。
> 方法：自顶向下——先 4A 架构层判定合理可用，再 DDD 战术建模、BDD 场景规格、TDD 测试金字塔，最后敏捷排期。编号接续现有体系（BA-BR-15+ / DA-T-14+ / DA-INV-07+ / SC-12+ / US-E8+），不新建平行编号。
> **v1.6 增补：统计异常检测建议线接线（G1-1/F1 完工，docs/13 v2.1）**——pyod 三检测工具（已在码，US-E12 注册）补契约编号 API-M-17~19 并接入调查闭环：planner 第五可选源 stat，**仅无特征保守全查路径纳入**（特征命中路径豁免留痕）；输出仅 advisory 参谋分入 findings 不驱动评分与处置（同 BR-16/BR-24 精神）；依赖缺失（pyod/numpy 未装）或样本不足自动跳过留痕不阻断主链（E-TOOL-UNAVAILABLE 降级分支）。业务含义：值班员多一个金额序列统计异常视角，只参谋不定案。编号接续：新增规则 BA-BR-25（建议线治理）、场景 SC-24（23→24）、排期 US-E16；零新表、零新事件、零状态机变更。
> **v1.7 增补：案件治理批次——赛道对标三缺口闭合（已实施，docs/09 v1.3）**——行业对标八条最佳实践净结论：五条已对齐、三条真缺口，本批次全部闭合：① 缺口#2 优先级队列（BA-BR-26）：队列按风险分级（high/mid/low，边界复用 BR-02/01）而非立案时序 + aging 滞留/超期富化（br-26-aging-hours 热配置），API-W-28；② 缺口#4 STR 叙事生成（BA-BR-27，docs/13 D2 闭合）：证据链唯一素材五段装配，引用 token（SIG/EV/DSP/APR + id 前 8 位）对齐防幻觉，注入产出未对齐降级规则轨，DRAFT 待人审 + 生成留痕，API-W-30；③ 缺口#5 可治理自动关闭（BA-BR-28）：零信号降噪归档叠加金额标准准入（br-28-auto-close-max-amount 热配置），关闭留痕带当时标准引用可复算，人工复位通道 ARCHIVED→MANUAL_REVIEW（human_only，事由必填，仅值班员/策略管理员），API-W-29。编号接续：新增规则 BA-BR-26~28（25→28）、场景 SC-25~27（24→27）、排期 US-E17~19；零新表零新态（新迁移对复用既有态，11-case-governance.sql 同步白名单与热配置种子），事件目录增 CaseReopened（web-api 扩展同 SC-10 先例）。
> 纪律：所有增强为**扩展**而非重构——12 态状态机骨架不变、LLM 无决策权不变、既有 6 不变量只增不改。

> **v1.1 增补 A0：角色边界强制（隐患修复，已实施）**——评审发现四角色菜单分化仅停留在前端展示层与数据库账号层，API 网关层无任何角色拦截：任何持令牌者自声明 X-Operator 即可调审批/知识发布/阈值配置端点，角色协同在 API 层仅为纸面约定。修复见 §0.1。

---

## 0. 总览：13 增强点的架构判定

| 增强 | 4A 判定 | 结论 |
| --- | --- | --- |
| A1 自适应基线 | BA 规则扩展 + DA 新表 + SK-01 内核扩展；无新中间件 | **采纳（P0）** |
| B3 并行假设 | AA planner 编排扩展；状态机不变（并行限于 INVESTIGATING 内部） | **采纳（P0）** |
| C1 控辩互审 | AA planner debate 模式 + 审计事件扩展；裁决权不变 | **采纳（P0）** |
| E1 知识代谢 | SK-05 扩展 + kb_document 增列；DA-INV-06 人工门不变 | **采纳（P0）** |
| B1 拓扑评分 | AA-MCP-01 工具参数扩展；新不变量锁"仅线索不决策" | **采纳（P1）** |
| B2 时序模式 | SK-01 规则扩展；纯规则无训练 | **采纳（P1）** |
| D1 审计预检 | audit API 只读扩展；不触碰状态机 | **采纳（P1）** |
| E2 规则进化 | kb 申请通道扩类型 + 人审门复用 | **采纳（P1）** |
| A2/C2/D2/F1 | 合理但属建议层/长窗/生态，依赖或排期靠后 | **排期 P2** |
| F2/F3 | 生态项，与代码解耦，可随时插入 | **排期 P2** |
| LoopEngine 环设施（v1.3） | AA EventWorker/planner/knowledge 编排扩展 + DA 驻车/归因两表；状态机不变（DLQ 不改案件状态）；无新中间件 | **采纳（P0）** |
| RAG 深化：案例分析语料 × B 端助手（v1.4） | AA-SK-04/05 复盘语料结构化 + web-api 问答端点 + AA-AG-06 新角色；零新表零新事件（复用 kb_document/kb_embedding） | **采纳（P0）** |
| 企业资质外部源双轨（v1.5） | AA-MCP-02 既有端口增 query_enterprise 工具 + planner 四源白名单 + ExternalSourcesClient 适配器；零新表零新事件零状态机变更 | **采纳（P0）** |

**整体影响评估**：新增 2 表（DA-T-14/15）、7 规则（BA-BR-15~21）、3 不变量（DA-INV-07~09）、4 领域事件、6 场景（SC-12~17）；**v1.3 再增**：2 表（DA-T-16/17）、1 规则（BA-BR-22）、1 事件（E-WORKER-DLQ）、1 列（risk_case.source_type）、2 场景（SC-19/20）；**v1.4 再增**：1 规则（BA-BR-23）、2 场景（SC-21/22）、1 端点（API-W-27）、1 Agent 角色（AA-AG-06）；**v1.5 再增**：1 规则（BA-BR-24）、1 场景（SC-23）、1 MCP 工具（API-M-16）；**零新中间件、零状态机变更、零既有不变量修改**——架构稳定性成立。

### 0.1 A0 角色边界强制（隐患修复 · 已实施）

**隐患识别**：角色分化的三层落地状况核查——

| 层 | 状况 | 证据 |
| --- | --- | --- |
| 数据库层 | ✅ 真实分化 | 02-roles.sql：tg_web/tg_app 读写矩阵、只增表 REVOKE |
| 前端页面层 | ⚠️ 纸面边界 | role.js/router.js 菜单与路由守卫仅展示过滤，绕过前端直达 API 即失效 |
| API 网关层 | ❌ 缺失（修复前） | api_guards.py 仅有全局 Bearer；X-Operator 仅审计记录，不校验角色权限 |

**修复设计**（api_guards.py 端点级 RBAC，对齐 03 §6 权限矩阵 × 01 §6 角色旅程）：

| 端点 | 允许角色 | 对齐依据 |
| --- | --- | --- |
| `/api/cases/*/review` | 风控审批官 | 人工复核唯一出口（02 §7 human_only） |
| `/api/approvals/*/decide`、`/api/config*` | 审批官 ∪ 策略管理员 | tg_web 的 approval_record/sys_config UPDATE 授权 |
| `/api/kb/applications/*/publish\|reject` | 策略管理员 | DA-INV-06 知识发布仅限人工且归策略线 |

**行为契约**：已知角色越权 → 403 `E-FORBIDDEN-ROLE` + `api.forbidden` 留痕；agent: 自声明穿透至端点 human_only 守卫 → 409 `E-HUMAN-ONLY`（语义分层：403=角色无权，409=业务门拒绝，与 07-case-actor-gate 对齐）；未识别调用方（历史测试名/MCP/CI）放行 + `api.unknown_actor` 留痕，收敛节奏与 R-37 一致。契约测试：test_multi_role_flow 流程 E（SC-18）。

**四角色 × 四专属工作台分化**（与 API 门控同源对齐，消除「共享工作台、按钮级差异」的归属错乱）：案件工作台归值班员专属（立案 → 信号聚合 → 调查取证 → 提请处置，复核 UI 整体剥离迁出）；复核审批工作台归审批官专属（`ApprovalPortal.vue` 重写为人工复核队列默认 tab + 审批工单双 tab，复核弹窗与 submitReview 自案件工作台迁入）；审计工作台归审计员专属（审计回放 + 结果核验）；策略工作台归策略管理员专属（知识审核发布）；可观测面板为唯一共享页。`router.js` meta.roles 白名单 + `App.vue` menus roles + HOME_BY_ROLE 三层同步，角色切换统一回各自首页。四角色菜单自此真实分化：值班员=案件工作台+可观测；审批官=复核审批工作台+可观测；审计员=审计工作台+可观测；策略管理员=策略工作台+可观测。

**端到端验证证据**（浏览器四角色接力，CASE-20260819-a60cd2 主线）：值班员受理告警立案 → 值班员越权调 review 复测 403（修复前实证 200）→ 审批官复核确认（`case.transition.ReviewConfirmed` actor=human:风控审批官）→ 审批官批准冻结（approver=human:风控审批官 → DispositionExecuted → DISPOSED）→ 审计员触发结果核验（VerificationPassed → ARCHIVED，复盘自动提入库申请 pending）→ 策略管理员审核发布复盘（reviewer=human:风控策略管理员 → published，DA-INV-06）。全量回归 252 过。

## 1. 4A 架构层

### 1.1 BA 业务架构：7 条新规则

| 编号 | 规则 | 承载能力 | 来源增强 |
| --- | --- | --- | --- |
| BA-BR-15 | 账户自适应基线偏离度进入评分，与全局阈值双轨取高 | BA-CAP-03 | A1 |
| BA-BR-16 | 图拓扑团伙分仅作调查线索，不得进入处置裁决 | BA-CAP-04 | B1 |
| BA-BR-17 | 资金回路/快进快出/夜间突发三时序模式进入聚合评分 | BA-CAP-03 | B2 |
| BA-BR-18 | 并行假设调查必须留痕「为什么没查 X」 | BA-CAP-04 | B3 |
| BA-BR-19 | 控辩辩论记录必须入审计，且不改变裁决权归属 | BA-CAP-05 | C1 |
| BA-BR-20 | 知识有效性降级自动、升级与发布人工 | BA-CAP-07 | E1 |
| BA-BR-21 | 规则提案（rule_proposal）经策略管理员审核方可生效 | BA-CAP-07 | E2 |
| BA-BR-22 | 环治理：失败归宿驻车 DLQ 不静默丢弃；复位仅限 human:*；环记录只增不删；自动环不消费 TEST 源 | 全部环能力 | LoopEngine v1.3 |
| BA-BR-23 | 问答治理：回答仅可引用已发布知识（doc_id 对齐），未命中声明“无先例”不虚构；问答仅对已识别人类角色开放且留痕可追责；归档复盘产结构化案例分析供检索复用 | RAG 语料面 + 消费面 | RAG 深化 v1.4 |
| BA-BR-24 | 企业资质外部源治理：调查深查源纳入企业五维，双轨集成（厂商 Key 在则真实轨，失败/无 Key 降级 mock 且 degraded 留痕）；输出仅线索不入评分与状态迁移；个人征信持牌红线保留 mock | 外部集成双轨 | 企业资质外部源 v1.5 |

### 1.2 AA 应用架构：组件扩展点

| 组件 | 扩展 | 增强 |
| --- | --- | --- |
| AA-SK-01 聚合内核 | baseline tracker（EWMA/分位窗口）+ temporal_patterns 匹配器 | A1/B2 |
| AA-SK-02 调查 / AA-AG-01 | planner 并行假设编排（≥2 假设并行深查、证据合并、反思对比） | B3 |
| AA-MCP-01 | `query_related_graph` 增 `topology_stats` 返回（星型/环型密度、二部集中度） | B1 |
| AA-SK-03 / planner | `review_disposition` 增 debate 模式（控/辩/裁三段） | C1 |
| AA-SK-04 审计 | audit API 增 `precheck`（专家清单预检，只读） | D1 |
| AA-SK-05 知识 | effectiveness 统计器 + rule_proposal 申请类型 | E1/E2 |
| AA-MCP-02 | pyod_* 工具族（3~5 检测器，仅建议输出，白名单门禁） | F1 |
| EventWorker / app/core/loop_engine.py | DLQ 失败归宿：累计重试上限驻车 + E-WORKER-DLQ 告知 + 轮询排除驻车与 TEST 源；/api/deadletter 查询与人工复位（守卫层角色白名单 + 端点 human_only 双重门控） | LoopEngine L1 |
| AA-SK-02 调查 / planner | replan_from_gaps 反思缺口二轮补查（MAX_REFLECT_ROUNDS=2，rounds 留痕） | LoopEngine L2 |
| AA-SK-05 知识 | attribute_rule_proposals 慢环归因（发布后再犯可度量，只增幂等） | LoopEngine L3 |
| AA-SK-04/05 核验沉淀 | _retrospective 升级结构化案例分析：四段（案件概况/手法指纹主型×分布/处置结论/复用提示），标题携主型“手法特征”检索锚点（与 AA-SK-02 检索词对齐） | RAG 语料面 v1.4 |
| web-api / AA-AG-06 | /api/kb/ask B 端问答端点（ask_kb：search_kb 复用 + 引用组装 + 无先例声明 + kb.ask 留痕）；AA-AG-06 知识助手 soul（B 端角色，只检索引用不裁决不处置不发布） | RAG 消费面 v1.4 |
| AA-MCP-02 mcp-external-mock / server.py | query_enterprise 五维工具（API-M-16）：确定性种子 mock 轨 + 厂商开放平台真实轨（防腐层字段翻译），异常降级 mock 且 degraded/vendor_error 留痕；query_reason 强制（BA-BR-10） | 企业资质外部源 v1.5 |
| AA-SK-02 调查 / planner × mcp_adapters | SOURCE_WHITELIST 四源（+enterprise）：仅无特征保守全查路径纳入，既有特征组合不动（豁免留痕）；ExternalSourcesClient.query_enterprise 适配器接线 | 企业资质外部源 v1.5 |

### 1.3 DA 数据架构：表/字段/事件

| 项 | 内容 | 增强 |
| --- | --- | --- |
| **DA-T-14 account_baseline**（新表） | account_id / window / ewma_amount / p95_amount / hour_histogram / updated_at；upsert 维护 | A1 |
| **DA-T-15 disposition_outcome**（新表） | case_id / t7_label / t30_label / recidivism_flag / appealed_flag；定时任务回填 | C2 |
| risk_signal.velocity_json | 增 `temporal_json`（三模式命中）与 `baseline_dev`（偏离度） | A1/B2 |
| kb_document | 增 cite_count / hit_correct / effectiveness_score 三列 | E1 |
| approval_record / audit_log | 增 debate_json（控辩裁三段，只增不改） | C1 |
| 领域事件 +4 | E-INV-HYPOTHESIS（并行假设生成）/ E-REVIEW-DEBATE / E-KB-DECAY（降级）/ E-OUTCOME-FOLLOW（T+7/30）——OpenAPI SseEvent 枚举逐字同步（契约测试守护） | B3/C1/E1/C2 |
| **DA-T-16 processing_deadletter**（新表，v1.3） | case_id PK / stage / error_class / error_msg / attempts / parked / resolved_by / resolved_at；累计达上限（缺省 9=3轮×3次）驻车停扫，人工经 /api/deadletter/{case_id}/retry 复位（只增不删） | LoopEngine L1 |
| **DA-T-17 proposal_attribution**（新表，v1.3） | doc_id PK / source_case_id / recurred_after / checked_at；规则提案发布后同主体再犯归因，幂等只增 | LoopEngine L3 |
| risk_case.source_type（增列，v1.3） | 案件来源类型落库（API-W-01 入参，缺省 'UNKNOWN' 兼容存量）；自动环 SQL 排除 TEST 源，合成案件归测试显式驱动（db/init/10-case-source.sql） | LoopEngine L1 |
| 环设施事件 +1（v1.3） | E-WORKER-DLQ（聚合环重试累计达 DLQ 上限驻车告知，actor=system:event-worker，可见性入口 /api/deadletter）——OpenAPI SseEvent 枚举同步 | LoopEngine L1 |
| kb_document 语料结构（v1.4，零新表零新列） | 复盘 content 升四段结构化案例分析（【案件概况】【手法指纹】【处置结论】【复用提示】），title 携主型锚点；发布仍走 DA-INV-06 人工门，向量化管道不变 | RAG 语料面 |
| audit_log action=kb.ask（v1.4，零新表） | B 端问答交互留痕（问题 + grounded 命中态，actor=human:角色，可追责到人） | RAG 消费面 |
| case_evidence 企业源留痕（v1.5，零新表零新列） | 企业五维载荷随 AG-01 计划证据链入 case_evidence（只增，DA-T-05）；评分与状态迁移入参不含 risk_flag | 企业资质外部源 |
| 归档复位事件 +1（v1.7，零新表零新态） | CaseReopened（ARCHIVED→MANUAL_REVIEW human_only 迁移事件，web-api 扩展同 SC-10 先例，26→27）——case_state_transition 白名单同步入 11-case-governance.sql；audit_log 治理留痕 case.auto_closed / narrative.generated（零新列） | 案件治理（BA-BR-26~28） |

### 1.4 TA 技术架构：选型与降级

| 决策 | 选型 | 替换路径 | 降级 |
| --- | --- | --- | --- |
| EWMA/分位基线 | 纯 Python + PG upsert（无新组件） | 换 Redis 滑动窗口（成本评估见 04 §2 格式） | 基线缺失回退全局阈值（BA-BR-15 双轨） |
| 拓扑统计 | 进程内内存计算（子图规模 ≤ 百节点） | 换 NetworkX/SQL 递归 | 计算超时返回空统计，调查不阻断 |
| PyOD 工具族 | optional extras 依赖，不进主 requirements | 换自研统计检测器 | 工具不可用即白名单拒绝，主链路无感 |
| 并行编排 | asyncio.gather（既有运行时） | — | 单假设失败不阻断其余，留痕豁免原因 |
| DLQ 驻车（v1.3） | PG 表 + 进程内环（不引入新 MQ DLQ topic） | 迁移 RocketMQ DLQ topic | 驻车不改案件状态（状态机仍是迁移权威）；复位双重人工门，驻车记录只增 |
| 双轮有界环（v1.3） | asyncio 同步编排（不引入新调度器） | — | 反思缺口仅触发一次二轮补查，上限即终止；无缺口单轮收敛不空转 |
| TEST 源隔离（v1.3） | source_type 落库 + 自动环 SQL 过滤（10-case-source.sql） | 独立测试库 | 合成案件归测试显式驱动，消除共享库轮询与测试迁移竞态（12 态状态机与不变量零变更） |
| B 端问答（v1.4） | 确定性模板组装（回答全部由检索结果构造，构造性防幻觉，无新 LLM 依赖） | 接入 LLM 合成端口后同路由可加 SSE 流式（契约不变） | 检索未命中即声明无先例；哈希 embedding baseline 无外部依赖（同 R-40 降级链） |
| 企业资质双轨（v1.5） | stdlib urllib 同步调用经 asyncio.to_thread 包装（无新依赖）；防腐层双命名兼容 + 缺省兜底 | 厂商 SDK/多供应商聚合 | 厂商异常即降级 mock（degraded 留痕，主链路无感）；无 Key 默认 mock 轨，测试/演示环境零外呼依赖 |

## 2. DDD 战术扩展

**新不变量（只增不改）：**

| 编号 | 不变量 | 守护测试层 |
| --- | --- | --- |
| DA-INV-07 | topology_stats 与 LLM 建议分不得直接驱动状态迁移（裁决仅规则内核 + 人工审批） | 单元（状态机白名单）+ 契约 |
| DA-INV-08 | rule_proposal 条目未经 human 审核不得进入规则库 | DB 触发器 + 集成 |
| DA-INV-09 | debate_json 与审计摘要只增不改（延伸 DA-INV-05 语义） | 权限矩阵测试 |

**聚合影响：** RiskCase 聚合增值对象 `HypothesisSet`（并行假设）与 `DebateRecord`（控辩裁）；KbDocument 聚合增 `Effectiveness` 值对象与 rule_proposal 申请类型；Disposition 聚合经 DA-T-15 以 ID 引用关联效果标签（跨聚合仍只以 ID 引用，docs/03 §9 纪律不变）。

**v1.3 LoopEngine 建模：** `ProcessingDeadletter` 为独立实体（仅以 case_id 引用 RiskCase，**不参与案件状态迁移**——状态机仍是迁移权威 DA-INV-01/02）；`ProposalAttribution` 为 KbDocument 聚合外挂值对象（经 doc_id/source_case_id 双向 ID 引用）。不变量**不新增编号、语义延伸**：复位人工门继承 DA-INV-06/08（人工门不被环绕过）语义，环记录只增继承 DA-INV-05/09（只增不改）语义，TEST 源排除属环消费策略而非领域不变量（落 BA-BR-22）。

**v1.4 RAG 深化建模：** KbDocument 聚合增内容结构规约（四段案例分析为语料质量标准，属聚合内约束而非新实体）；问答为只读应用服务（读 published 可见面，DA-INV-06 发布人工门不变，问答不产生任何聚合写）。AA-AG-06 为 B 端服务角色（AgentTeams 首次从流水线内部 worker 向外扩展）：职能边界只检索引用，不裁决/不处置/不发布，行为约束落 soul 与端点双重守护（BR-23）。

**v1.5 企业资质外部源建模：** EnterpriseProfile 为外部源防腐层值对象（五维 + 合成 risk_flag，由厂商字段翻译或 mock 种子生成，不入任何聚合持久态，仅随证据链留痕）；双轨为 AA-MCP-02 端口内实现替换（调用链路与生产一致，真实集成是换实现不是建新路）；线索不裁决继承 DA-INV-07/BA-BR-16 语义（不新增不变量编号）。合规边界落 BA-BR-24：个人征信持牌红线保留 mock，企业资质为可合规接入点。

## 3. BDD 场景扩展（SC-12~23）

| 场景 | Given-When-Then 摘要 | 追溯 |
| --- | --- | --- |
| SC-12 自适应基线捕获渐进盗用 | Given 账户 30 天小额平稳基线；When 突增 8 倍交易；Then 偏离度入评分且分档上调，全局阈值未触发亦入中通道 | BR-15 · INV-01 |
| SC-13 拓扑分命中团伙但不驱动处置 | Given 同设备 5 账户二部子图；When 调查查询；Then topology_stats 输出高嫌疑分且案件状态迁移不因该分发生 | BR-16 · INV-07 |
| SC-14 时序回路命中跑分剧本 | Given A→B→C→A 90 分钟内闭环；When 聚合；Then temporal_json 命中回路模式，评分上调 | BR-17 |
| SC-15 并行假设留痕完备 | Given 高风险案 ≥2 假设；When 调查完成；Then trace 含并行分支与「为什么没查 X」豁免留痕 | BR-18 |
| SC-16 控辩辩论入审计不改裁决 | Given 冻结建议；When 互审；Then 审计含控/辩/裁三段，最终裁决仍由审批官作出 | BR-19 · INV-09 |
| SC-17 知识降级自动、发布人工 | Given 条目 30 天零引用；When 统计任务运行；Then 自动转 pending；When Agent 试图直接 published；Then 被拒（INV-06/08） | BR-20/21 · INV-06/08 |
| SC-18 角色边界 API 层强制（A0） | Given 值班员持有效令牌；When 调审批/发布/配置端点；Then 403 E-FORBIDDEN-ROLE 且 api.forbidden 留痕；When 未识别调用方；Then 放行 + api.unknown_actor 留痕 | 03 §6 · BR-09 |
| SC-19 环失败归宿驻车与复位人工门（L1） | Given 环重试耗尽；When 累计达上限；Then 驻车 + E-WORKER-DLQ + 轮询排除；When agent/越权复位；Then 409/403；When 值班员复位；Then 清零放行 resolved_by=human:*（只增不删） | BR-22 · INV-06/08 延伸 |
| SC-20 双轮有界环不空转与慢环归因（L2/L3） | Given 首选源首轮降级；When 调查反思 gaps；Then 二轮仅补查降级源，rounds 留痕 2 轮；Given 全部源首轮成功；Then 一轮终止；Given 提案人审发布；When 同主体再犯；Then recurred_after=True（归因幂等只增） | BR-22 · INV-05/09 延伸 |
| SC-21 归档复盘产结构化案例分析且可检索复用（语料面） | Given 案件全链归档；When 复盘申请产出；Then 四段结构化（手法指纹主型×分布）；When 人审发布；Then 以主型“手法特征”检索即命中该复盘 | BR-23 · INV-06 延伸 |
| SC-22 B 端问答引用守护与人工角色门（消费面） | Given 已发布知识；When 人工角色问同类问题；Then grounded + citations 含 doc_id；When 问无关联；Then 声明无先例；When agent 调用；Then 403 且 kb.ask 留痕 | BR-23 |
| SC-23 企业资质外部源五维扩维（双轨集成） | Given 无特征案件（保守全查）；When 调查执行；Then enterprise 五维齐备入 findings 与证据链且评分不受 risk_flag 影响；When query_reason 缺失；Then E-REASON-REQUIRED；Then 同主体确定性回放一致（无 Key 默认 mock 轨） | BR-24 · BR-10 |

## 4. TDD 测试金字塔映射

| 层 | 新增用例 | 承载 |
| --- | --- | --- |
| 单元 | EWMA/分位计算、三时序模式匹配、拓扑统计（星/环/二部）、effectiveness 统计 | test_aggregation / 新 test_topology / test_baseline |
| 契约 | topology_stats 工具 Schema、debate 输出 Schema、precheck Schema、SseEvent 4 新事件枚举逐字 | test_mcp_gate / test_routes_contract |
| 集成 | baseline upsert 与双轨评分、planner 并行编排、outcome 回填任务、知识代谢任务 | test_event_worker / test_planner / test_knowledge |
| 场景 E2E | SC-12~17 真实链路；SC-18 角色门控（已实施：test_multi_role_flow 流程 E） | test_scenario_matrix 增 6 行 |
| 评估 | A1 A/B 召回对比（渐进盗用剧本）、KPI 增「处置后 30 天再犯率」口径 | dataset_replay / kpi_report 扩展 |
| **v1.3 LoopEngine（已实施）** | 单元：replan_from_gaps 可行动源判定（纯函数）；契约：deadletter 双重人工门 409/403 + OpenAPI /api/deadletter 双 path；集成：DLQ 生命周期/驻车排除/双轮补查/慢环归因 14 例；场景：SC-19/20 | test_loop_engine（14 例）/ test_scenario_matrix 增 2 行 |
| **v1.4 RAG 深化（已实施）** | 单元：结构化复盘四段与无信号降级声明；契约：OpenAPI /api/kb/ask path（API-W-27）；场景：SC-21（全链归档→四段→发布即命中）/SC-22（grounded/无先例/403/留痕） | test_verification 增 1 例 / test_scenario_matrix 增 2 行 |
| **v1.5 企业资质外部源（已实施）** | 单元：execute_plan enterprise 源分派与五维载荷；契约：OpenAPI McpQueryEnterpriseReq/Resp（API-M-16，x-mcp-tool 对齐）；场景：SC-23（活栈 AA-MCP-02 四源全查/线索不裁决/事由门/确定性回放） | test_planner 增 1 例 / test_scenario_matrix 增 1 行 |

## 5. 敏捷排期（US-E8~E15，先 P0 后 P1/P2）

| 里程碑 | Stories（单条 ≤4h） | 验收 | 依赖 |
| --- | --- | --- | --- |
| **US-E8 自适应基线与时序模式**（Sprint 1，✅ 已实施） | E8-01 DA-T-14 表与 EWMA updater；E8-02 双轨评分接线（BR-15）；E8-03 三模式匹配器（BR-17）；E8-04 SC-12/14 场景测试 | SC-12/14 绿 + A/B 报告 | 无 |
| **US-E9 图拓扑与并行调查**（Sprint 2，✅ 已实施） | E9-01 topology_stats 工具 + 契约；E9-02 planner 并行编排 + 豁免留痕；E9-03 SC-13/15 测试 | SC-13/15 绿 | E8（评分框架） |
| **US-E10 控辩互审与审计预检**（Sprint 3，✅ 已实施） | E10-01 debate 模式；E10-02 precheck API + 门户展示；E10-03 SC-16 + 预检测试 | SC-16 绿 | 无（可与 E9 并行） |
| **US-E11 知识代谢与规则进化**（Sprint 4，✅ 已实施） | E11-01 effectiveness 统计与自动降级；E11-02 rule_proposal 通道 + 人审门；E11-03 SC-17 测试 | SC-17 绿 | 无 |
| **US-E12 生态与长窗**（Sprint 5-6，P2，✅ 已实施） | E12-01 outcome 回填任务（C2）；E12-02 pyod 工具族（F1）；E12-03 registry 发布 + 互操作报告（F2/F3） | KPI 新口径 + 报告入档 | E8~E11 |
| **US-E13 LoopEngine 环设施**（Sprint 7，✅ 已实施，v1.3） | E13-01 DA-T-16/17 两表与 loop_engine DLQ 生命周期（累计/驻车/复位只增）；E13-02 EventWorker 接线（重试耗尽接 DLQ + E-WORKER-DLQ + 轮询排除驻车与 TEST 源）；E13-03 双轮有界环 replan_from_gaps + rounds 留痕；E13-04 慢环归因 attribute_rule_proposals + /api/deadletter 双重人工门端点与契约 | SC-19/20 绿 + test_loop_engine 14 例全绿 | 无（与主线并行） |
| **US-E14 RAG 深化：语料 × B 端助手**（Sprint 8，✅ 已实施，v1.4） | E14-01 _retrospective 结构化案例分析四段（主型×分布检索锚点）；E14-02 /api/kb/ask 问答端点（引用组装 + 无先例声明 + 人工角色门 + kb.ask 留痕）；E14-03 aa-ag-06 知识助手 soul + agentteams 脚本接入；E14-04 SC-21/22 场景测试 | SC-21/22 绿 + 结构化复盘单测绿 | 无（复用既有 RAG 管道） |
| **US-E15 企业资质外部源双轨**（Sprint 9，✅ 已实施，v1.5） | E15-01 query_enterprise 五维工具（确定性 mock 种子 + 厂商真实轨防腐层，异常降级留痕）；E15-02 planner 四源白名单接线（仅无特征保守全查路径纳入）+ ExternalSourcesClient 适配器；E15-03 OpenAPI 契约（API-M-16）；E15-04 SC-23 场景测试（活栈 AA-MCP-02） | SC-23 绿 + 分派单测绿 | 无（AA-MCP-02 既有端口） |
| **US-E16 统计异常检测建议线接线（G1-1/F1 完工）**（Sprint 10，v1.6） | E16-01 pyod 三工具补契约编号 API-M-17~19（openapi McpQueryStatReq/Resp，工具代码已在码零改动）；E16-02 planner 第五可选源 stat 接线（仅无特征保守全查路径 + 特征命中豁免留痕 + 样本不足/依赖缺失跳过留痕不阻断）+ ExternalSourcesClient.query_stat_outliers 适配器；E16-03 test_planner 分派/降级单测；E16-04 SC-24 场景测试（活栈 AA-MCP-02） | SC-24 绿 + 分派/降级单测绿 + 全量回归基线抬升 | 无（工具已在码，optional extras 缺失走降级分支） |
| **US-E17~19 案件治理批次（赛道对标三缺口闭合）**（Sprint 11，✅ 已实施，v1.7） | E17-01 case_governance 纯函数（分级/aging/自动关闭准入）+ CaseRepository.queue + /api/cases/queue 富化端点（API-W-28，br-26-aging-hours 热配置）；E18-01 narrative 引用对齐装配/校验门/双轨降级 + /api/cases/{id}/narrative 人工角色门（API-W-30，docs/13 D2 闭合）；E19-01 noise 分支自动关闭准入 + case.auto_closed 标准引用留痕 + CaseReopened 迁移（ARCHIVED→MANUAL_REVIEW human_only）+ /api/cases/{id}/reopen 角色门（API-W-29）+ 11-case-governance.sql 白名单/种子；E17~19-04 SC-25~27 + test_case_governance 11 例 | SC-25~27 绿 + 治理单测 11 例绿 + 全量回归基线抬升 | 无（复用 noise 通道与既有态） |

**排期纪律**：每 story 独立可验证（单测即验）；E8→E9 串行（评分框架先行），E10/E11 可与主线并行；P2 不阻塞 P0/P1 交付。

## 6. 追溯矩阵扩展（docs/06 §4 增 11 行）

| BDD 场景 | BA 规则 | DA 不变量 | 测试层 |
| --- | --- | --- | --- |
| SC-12 | BR-15 | INV-01 | 单元+场景 |
| SC-13 | BR-16 | INV-07 | 契约+场景 |
| SC-14 | BR-17 | INV-01 | 单元+场景 |
| SC-15 | BR-18 | INV-01 | 集成 |
| SC-16 | BR-19 | INV-09 | 集成+场景 |
| SC-17 | BR-20/21 | INV-06/08 | 集成+场景 |
| SC-18 | 03 §6 × BR-09 | —（A0 已实施） | 契约（流程 E） |
| SC-19 | BR-22 | INV-06/08 延伸 | 集成+场景 |
| SC-20 | BR-22 | INV-05/09 延伸 | 集成+场景 |
| SC-21 | BR-23 | INV-06 延伸 | 单元+场景 |
| SC-22 | BR-23 | —（端点约束） | 场景 |
| SC-25 | BR-26 | —（读面治理） | 单元+场景 |
| SC-26 | BR-27 | INV-06 延伸（DRAFT 待人审） | 单元+场景 |
| SC-27 | BR-28 | INV-01（白名单同步） | 单元+场景 |
