fastapi==0.141.1
# R-37：uvicorn 0.34.3 为官方安全修订线；mcp≥1.23.0 修复 CVE-2025-66416（DNS rebinding）；
# pydantic 随 mcp 1.29.0 下限（>=2.11.0）对齐升级
# R-37 复审收口：fastapi 0.115.6 将 starlette 锁死在 <0.42 脆弱区（7 枚已知 CVE，
# 含 CVE-2026-48710 Host 头污染鉴权绕过，≥1.0.1 修复；DoS 两枚 ≥1.3.1 修复）——
# 升 0.141.1 解析 starlette 1.6.0 全修复线，并显式钉版防传递依赖漂移；
# annotated-doc 为 fastapi 0.141 新增传递依赖，同源钉版（供应链可复现）。
# 应用层另备 scope["path"] 守卫（api_guards.py），双层不依赖单一修复面。
starlette==1.6.0
annotated-doc==0.0.5
uvicorn[standard]==0.34.3
asyncpg==0.30.0
pydantic==2.13.4
mcp==1.29.0
# 工作流 A2 预检结论（2026-08-14，python:3.12-slim 实测 PRECHECK-PASS）：
# 稳定线 2.0.0 仅 sdist（需 C++ 工具链），cp312 唯一可装轮子为 0.5.0rc2
# （py2.py3-none-manylinux1，内置 librocketmq，与 5.x broker 走 remoting 协议兼容）。
# events.py 仍保留 try/except 降级：本地无该库时 SSE fan-out 不断链（MQ 投递尽力而为）。
rocketmq-client-python==0.5.0rc2
