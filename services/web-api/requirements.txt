# find-links 内置指令：rocketmq wheel vendored 于 wheels/（相对本文件目录解析）。
# 原在 CI pip-audit 命令行传 --find-links，pip-audit 2.10 argparse 与 -r 互斥
# （报 project_path 冲突，线上实证），移入此处（pip 全局选项语法，pip-audit 内部
# pip 解析链路同样生效；docker build 的 pip install -r 也自动复用，行为不变）。
--find-links ./wheels
fastapi==0.141.1
# R-37: uvicorn 0.34.3 official security line; mcp>=1.23.0 fixes CVE-2025-66416 (DNS rebinding);
# pydantic aligned with mcp 1.29.0 lower bound (>=2.11.0).
# R-37 re-review: fastapi 0.115.6 pinned starlette <0.42 vulnerable zone (7 known CVEs incl.
# CVE-2026-48710 Host-header auth bypass fixed in >=1.0.1; two DoS fixed in >=1.3.1) --
# upgrade to 0.141.1 resolves starlette 1.6.0 fully-patched line, explicit pins prevent
# transitive drift; annotated-doc is a fastapi 0.141 transitive dep pinned from same source.
# App-layer scope["path"] guard (api_guards.py) provides defense-in-depth.
starlette==1.6.0
annotated-doc==0.0.5
uvicorn[standard]==0.34.3
asyncpg==0.30.0
pydantic==2.13.4
mcp==1.29.0
# Workflow A2 precheck (2026-08-14, python:3.12-slim PRECHECK-PASS):
# stable 2.0.0 is sdist-only (needs C++ toolchain); 0.5.0rc2 wheel was the only
# installable cp312 line (bundled librocketmq, remoting-protocol compatible with 5.x broker).
# 2026-08-18: PyPI removed the 0.5.0rc2 manylinux wheel (successors are sdist-only) --
# vendored at services/web-api/wheels/ (see wheels/README.md); install with
#   pip install --find-links=services/web-api/wheels -r requirements.txt
# events.py keeps try/except fallback: without this lib SSE fan-out stays alive (MQ best-effort).
rocketmq-client-python==0.5.0rc2
