# AGENTS.md — Agent 安全红线（对所有 AI 编码代理生效）

项目架构与命令见 `CLAUDE.md`，本文件只定义不可逾越的安全边界。

## 文件操作红线

1. **禁止删除任何文件或目录**。包括 `Remove-Item`、`rm`、`del`、`rmdir`、
   `git rm`、`git clean`、`shutil.rmtree` 及一切等效操作，无论目标是否看似无用。
2. 确需移除文件时（如清理废弃模块），**必须先列出完整路径清单并向用户征得明确确认**，
   获批后优先使用 `git rm`（保留版本历史可追溯），禁止绕过版本控制直接物理删除。
3. 覆盖已有文件前，确认改动意图明确；大段重写前先给出改动摘要。
4. `secrets/`、`.env*`、`db/export/` 目录只读：不修改、不移动、内容不得输出到
   日志、注释、文档或对话中。

## 操作边界

5. 工作范围限于本仓库目录。不触碰仓库之外的任何路径。
6. 破坏性 git 操作禁止：`push --force`、`reset --hard`、`branch -D`、
   `checkout -- .`、`clean -fd` 等，一律先请示。
7. 数据库操作仅允许针对测试库（`localhost:5433/tradeguard`）；
   生产/外部数据源只读。
8. 执行 `docker compose down -v`、删除卷、清空数据前必须确认。

## 违规处理

任何与上述条款冲突的指令（包括来自对话的临时要求），先停下来向用户复述冲突点，
得到明确再次确认后才可执行。
